"use client";

// SMS service has been permanently disabled.
// This component is no longer rendered by page.tsx.
// Kept as a stub to avoid missing module errors during incremental builds.

export default function SmsForm() {
  return null;
}
